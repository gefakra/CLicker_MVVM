<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="Clicker_MVVM.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:ClickerMVVM"
    Shell.FlyoutBehavior="Disabled"
    Title="ClickerMVVM">

    <TabBar>
        <ShellContent Title="Clicker" ContentTemplate="{DataTemplate local:ClickerPage}" />
        <ShellContent Title="Stats" ContentTemplate="{DataTemplate local:StatsPage}" />
        <ShellContent Title="Stocks" ContentTemplate="{DataTemplate local:StocksPage}" />
    </TabBar>

</Shell>
